``  position before jump       '' same BOL
`.  go to last edit            '. same BOL
g;  go edits backwards         g, same forwards
`^  go where INSERT was left   gi same and start INSERT
mx  set mark x                 `x  go to mark x

0  BOL blank        $  EOL blank
^  BOL non-blank    g_ EOL non-blank
fx go to x in line  tx go before x in line

%  jump between brackets in line
{  jump block up              
}  jump block down

map [[ ?{<CR>w99[{
map ][ /}<CR>b99]}
map ]] j0[[%/{<CR>
map [] k$][%?}<CR>

diw  delete inner word        daw  delete a    word 
di(  delete inner (content)   da(  delete all  (content)

same concept for these:  []  {}  ''  ""

ca(  same and start INSERT mode
ci(  same and start INSERT mode

TODO

c-o / c-i switch between last jumps
*  do a / search for the exact word under cursor, other direction use #

d/xyz delete until xyz

dd   delete line
S-D  delete until EOL
S-C  deleteuntil EOL and start INSERT MODE
cc DEL line and start INSERT MODE

S-R to replace text -> use backspace to revert

===== show whitespace, tabs etc. as symbols =====

set list
set listchars=tab:▸\ ,trail:·,extends:>,precedes:<,space:·

TURN OFF AGAIN:
set nolist
OR
set list!
